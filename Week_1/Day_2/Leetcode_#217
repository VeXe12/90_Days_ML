class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        my_set = set()
        output = 0
        for i in range(len(nums)):
            if nums[i] not in my_set:
                my_set.add(nums[i])
            else:
                output += 1
                break
        
        if output == 1:
            return True 
        else:
            return False 